# Registry of every n8n workflow this repository knows about.
#
# The LOGICAL ID is the primary key. A workflow name is edited in the n8n UI and a remote id is
# per-environment, so neither is an identity. See automation/n8n/conventions.md.
#
# lifecycle:
#   managed   — repository owns it: workflow.json + manifest + contracts committed
#   observed  — inventoried and documented, artifact not yet imported (mid-migration)
#   orphan    — exists on the instance, nobody has claimed it
#
# `pnpm n8n:inventory` reconciles this file against the live instance.
# `pnpm n8n:validate` checks it offline, on every pull request.

schemaVersion: 1

environments:
  development:
    # No development n8n instance exists yet. Until one does, every write path stays closed
    # (docs/reference/n8n/environments.md).
    available: false
  production:
    available: true
    # The MCP token in .env.local points here. Read-only by policy.
    writable: false

workflows:
  - id: ooo-detect-and-log
    name: "[child-3] TAG_ATTACHED · OOO · Detect return date and log"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-detect-and-log
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: O4DqMEu1Z9LcxikE
    lastRepositorySync: 2026-07-21
    lastValidation: 2026-07-21 pnpm n8n:validate — 0 errors, accepted violations recorded in manifest

  # The rest of the OOO family. Imported as `imported`: the graph is committed and diffable, the
  # documentation is not written yet. See docs/reference/n8n/ooo-phase-a.md.
  - id: ooo-enrol-followups
    name: "Add OOO Leads"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-enrol-followups
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # the only OOO workflow that calls a Bison WRITE endpoint
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: zaPkpSAuvjibUUDU
    lastRepositorySync: 2026-07-21

  - id: ooo-remove-on-tag-removed
    name: "[child-7] TAG_REMOVED · OOO · Remove from OOO Leads"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-remove-on-tag-removed
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: ZZ0ughB302WdDJOf
    lastRepositorySync: 2026-07-21
    lastValidation: 2026-07-21 phase A branch S published

  # LinkedIn channel (Aimfox). All five imported; phase 0 — no Supabase branch exists yet.
  # The three lead/classification workflows became importable on 2026-07-22, once their literal
  # secrets moved into the `Aimfox Master` and `OpenAi account` credentials (security.md §7, §8).
  - id: aimfox-daily-metrics
    name: "Get Metrics from Aimfox"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/aimfox-daily-metrics
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: sVev5d0N6rtrbcgI
    lastRepositorySync: 2026-07-21

  - id: aimfox-import-to-connection
    name: "Import leads to Aimfox connection"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-import-to-connection
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # POSTs to a campaign audience — queues real LinkedIn invites
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: nG6Q4KEGeXk7tBHm
    lastRepositorySync: 2026-07-21

  - id: aimfox-classification
    name: "AimFox Classification"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-classification
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # blacklisting is irreversible for that client
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: JnvRBXtRNar7ejeM
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 literal OpenAI key replaced by the OpenAi account credential

  - id: aimfox-premql-to-pdca
    name: "preMGL tag added (Aimfox) -> Add lead to PDCA"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-premql-to-pdca
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # creates leads
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: s0GqDtCzyLAvVnm1
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 literal Aimfox token replaced by the Aimfox Master credential

  - id: aimfox-leads-processing
    name: "AimFox Leads Processing"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-leads-processing
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # creates leads AND dispatches them to the clients' CRMs
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 4OjNRWLaG2IWK6kd
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 literal Aimfox token replaced by the Aimfox Master credential

  - id: nrr-daily-stats
    name: "[child-2] TAG_ATTACHED · NRR · Daily stats"
    domain: outreach
    path: automation/n8n/workflows/outreach/nrr-daily-stats
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: low
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 1hHbU2hYYcsktLUP
    lastRepositorySync: 2026-07-21
