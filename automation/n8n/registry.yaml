# Registry of every n8n workflow this repository knows about.
#
# The LOGICAL ID is the primary key. A workflow name is edited in the n8n UI and a remote id is
# per-environment, so neither is an identity. See automation/n8n/conventions.md.
#
# lifecycle:
#   managed   — repository owns it: workflow.json + manifest + contracts committed
#   observed  — inventoried and documented, artifact not yet imported (mid-migration)
#   orphan    — exists on the instance, nobody has claimed it
#
# `pnpm n8n:inventory` reconciles this file against the live instance.
# `pnpm n8n:validate` checks it offline, on every pull request.

schemaVersion: 1

environments:
  development:
    # No development n8n instance exists yet. Until one does, every write path stays closed
    # (docs/reference/n8n/environments.md).
    available: false
  production:
    available: true
    # The MCP token in .env.local points here. Read-only by policy.
    writable: false

workflows:
  - id: ooo-detect-and-log
    name: "[child-3] TAG_ATTACHED · OOO · Detect return date and log"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-detect-and-log
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: O4DqMEu1Z9LcxikE
    lastRepositorySync: 2026-07-21
    lastValidation: 2026-07-21 pnpm n8n:validate — 0 errors, accepted violations recorded in manifest

  # The rest of the OOO family. Imported as `imported`: the graph is committed and diffable, the
  # documentation is not written yet. See docs/reference/n8n/ooo-phase-a.md.
  - id: ooo-enrol-followups
    name: "Add OOO Leads"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-enrol-followups
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # the only OOO workflow that calls a Bison WRITE endpoint
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: zaPkpSAuvjibUUDU
    lastRepositorySync: 2026-07-21

  - id: ooo-remove-on-tag-removed
    name: "[child-7] TAG_REMOVED · OOO · Remove from OOO Leads"
    domain: outreach
    path: automation/n8n/workflows/outreach/ooo-remove-on-tag-removed
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: ZZ0ughB302WdDJOf
    lastRepositorySync: 2026-07-21
    lastValidation: 2026-07-21 phase A branch S published

  # LinkedIn channel (Aimfox). All five imported; phase 0 — no Supabase branch exists yet.
  # The three lead/classification workflows became importable on 2026-07-22, once their literal
  # secrets moved into the `Aimfox Master` and `OpenAi account` credentials (security.md §7, §8).
  - id: aimfox-daily-metrics
    name: "Get Metrics from Aimfox"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/aimfox-daily-metrics
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: sVev5d0N6rtrbcgI
    lastRepositorySync: 2026-07-21

  # The campaign catalog for the LinkedIn channel — mirrors bison-campaign-sync. Created 2026-07-22 so
  # promote_contact_to_lead can resolve an Aimfox campaign_id instead of leaving it NULL like the 30
  # backfilled leads. type='outreach' → clients see LinkedIn campaigns (ADR-0003).
  - id: aimfox-campaign-sync
    name: "Aimfox campaign sync"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/aimfox-campaign-sync
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # `campaigns` is the spine every campaign metric hangs off
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: t6a53dLc85FOKFqX
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 created inactive; upsert SQL proven in a rolled-back transaction (type/sequencer_id INSERT-only)

  - id: aimfox-import-to-connection
    name: "Import leads to Aimfox connection"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-import-to-connection
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: deprecated       # deactivated 2026-08-10; replaced by aimfox-import-to-connection-shadow
    criticality: high        # POSTed to a campaign audience while it was on
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: nG6Q4KEGeXk7tBHm
    lastRepositorySync: 2026-08-10
    lastValidation: 2026-08-10 measured importing for 0 of 9 clients, then deactivated

  - id: aimfox-import-to-connection-shadow
    name: "Aimfox AutoConnect import · Supabase"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-import-to-connection-shadow
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # POSTs to a campaign audience — queues real LinkedIn invites
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: akJ7RlG5Oog4Kxcb
    lastRepositorySync: 2026-08-10
    lastValidation: 2026-08-10 cutover — send limb live, schedule on; Sheets branch deactivated the same day

  - id: aimfox-classification
    name: "AimFox Classification"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-classification
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # blacklisting is irreversible for that client
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: JnvRBXtRNar7ejeM
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 literal OpenAI key replaced by the OpenAi account credential

  - id: aimfox-premql-to-pdca
    name: "preMQL tag added (Aimfox) -> Add lead to PDCA"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-premql-to-pdca
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # creates leads
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: s0GqDtCzyLAvVnm1
    lastRepositorySync: 2026-08-05
    lastValidation: 2026-08-05 exported the post-promotion blacklist chain added on the instance

  - id: aimfox-leads-processing
    name: "AimFox Leads Processing"
    domain: outreach
    path: automation/n8n/workflows/outreach/aimfox-leads-processing
    businessProcess: docs/reference/processes/outreach/linkedin-aimfox.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # creates leads AND dispatches them to the clients' CRMs
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 4OjNRWLaG2IWK6kd
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 literal Aimfox token replaced by the Aimfox Master credential

  - id: bison-lead-enrichment
    name: "[child-1] TAG_ATTACHED · Interested/PreMQL · Full enrichment"
    domain: outreach
    path: automation/n8n/workflows/outreach/bison-lead-enrichment
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # creates leads and dispatches them to the clients' CRMs
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: lBOyL8ZPA3SZSvDW
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 phase A branch S published — RPC chain verified in a rolled-back transaction

  # Imported 2026-08-15 by the instance audit: it was failing 211 of 337 runs and, being an orphan,
  # reported that nowhere. Sheets-only — no Supabase path exists yet (defect-backlog.md B2 / §5.1).
  - id: bison-blacklist-add
    name: "[child-4] TAG_ATTACHED · Blacklist add (email + domain)"
    domain: outreach
    path: automation/n8n/workflows/outreach/bison-blacklist-add
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high        # blacklisting is irreversible for that client
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: bEB3aOHEq2lEpubp
    lastRepositorySync: 2026-08-21
    lastValidation: >-
      2026-08-15 imported; scanner clean, 0 credential refs in the graph.
      2026-08-21 `tag != Not Interested (domain path)` had no `conditions.options`, so the domain
      limb threw on every run and `Domain Blacklist ID` was never written back to Sheets — the same
      defect as bison-ooo-campaign-revive (defect-backlog B9), found by auditing every
      if/filter/switch in the repository. One node re-deployed, drift 0.

  # Ops. Bound as settings.errorWorkflow on every managed workflow above.
  - id: automation-failure-recorder
    name: "[ERR] Automation failure recorder"
    domain: ops
    path: automation/n8n/workflows/ops/automation-failure-recorder
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: error
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: Pmz0JjRRuJNdNpSE
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 proved end to end — a deliberate failure produced one integration_sync_runs row

  # Bison ingestion — the counters every portal dashboard reads. Imported 2026-07-22 in the same
  # change that repaired them: all three top-level workflows had been failing on the dropped
  # clients.external_* columns (ADR-0012). See processes/outreach/bison-ingestion.md.
  - id: bison-campaign-sync
    name: "Bison campaign sync"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/bison-campaign-sync
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: UXpSOrgsN2TxjXUu
    lastRepositorySync: 2026-07-22
    lastValidation: >-
      2026-07-22 repaired and executed — 16 clients, 390 campaigns.
      2026-08-14 the OOO follow-up campaigns are classified by name at INSERT. `ooo_followup` was
      not a value this workflow could emit, so every one of them arrived misclassified — as
      `nurture` (unroutable), or as client-visible `outreach` for 25 of them — until 20260722g
      repaired 52 rows by hand. Proven offline (3 vendor types × 7 production names, lookalikes
      excluded) and by executing the generated SQL twice against Postgres; type survives an update
      that moves status and both counters. Not yet deployed.

  - id: bison-ooo-campaign-revive
    name: "[OPS] Bison OOO campaigns · revive paused"
    domain: ops
    path: automation/n8n/workflows/ops/bison-ooo-campaign-revive
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 1NNq86xVXbWkEnnF
    lastRepositorySync: 2026-08-21
    lastValidation: >-
      2026-08-20 built, never executed. The Bison endpoint surface was probed (GET/OPTIONS only):
      /resume, /pause, /archive and /duplicate exist, /unarchive and /restore return 404, and
      /campaigns/{id} allows no PATCH.
      2026-08-20 DRY RUN against production — the real Decide Action node over real vendor
      responses, no PATCH sent. 21 candidates (GIC's three correctly excluded by the
      auto_ooo_enabled guard): 3 would resume, all UniTalk and all `paused`; 18 cannot, all
      `archived`; 0 unreadable, 0 already active. Activating it would release 368 contacts in one
      go, and Bison will NOT pace that — both max_emails_per_day and max_new_leads_per_day are
      1000 on all three campaigns. Still not activated; that release needs a decision of its own.
      2026-08-20 12:40 UTC created as 1NNq86xVXbWkEnnF (inactive, both Postgres nodes given the
      credential of bison-workspace-setup's Record node) and ACTIVATED at the owner's direction,
      knowing the first run releases 368 contacts at once with no vendor-side pacing. The export
      round-tripped byte-identical. First fire: 2026-08-21 06:40 UTC — nothing runs today.
      2026-08-21 that first fire FAILED (execution 81115): `Needs Resume?` carried no
      `conditions.options`, so IfV2 threw "Cannot read properties of undefined (reading
      'caseSensitive')" on the first item — before the resume and before the audit row, leaving
      integration_sync_runs empty. Everything upstream was correct: 21 read, 18 archived, UniTalk's
      606/607/608 paused with 368 queued. One node re-deployed the same day, drift 0; CI now rejects
      the shape (structure/filter-options). Still never completed a run — see defect-backlog B9.

  - id: bison-campaign-daily-stats
    name: "Bison campaign daily stats"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/bison-campaign-daily-stats
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: AEgpCGoSpiZ7PA90
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 repaired — 416 campaigns resolve

  - id: bison-daily-stats-population
    name: "Bison daily stats population"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/bison-daily-stats-population
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: amJdB2eGXxUNyCPY
    lastRepositorySync: 2026-07-22
    lastValidation: 2026-07-22 repaired and executed — daily_stats written for 2026-07-22

  - id: bison-daily-stats-process
    name: "Daily Stats Process"
    domain: ingestion
    path: automation/n8n/workflows/ingestion/bison-daily-stats-process
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: BQbFKHUaIcEKPc01
    lastRepositorySync: 2026-07-22

  # Slack alerting for failures. Deliberately separate from automation-failure-recorder: that one
  # RECORDS a failure as a row, this one SHOUTS. n8n allows exactly one errorWorkflow per workflow,
  # so only one of the two can be bound — see the README for how that is resolved.
  - id: error-notification-slack
    name: "Error Notification"
    domain: ops
    path: automation/n8n/workflows/ops/error-notification-slack
    businessProcess: docs/reference/n8n/README.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: error
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 4jIUZMYNgKtb9fmi
    lastRepositorySync: 2026-08-18

  # The reply classifier: every Bison reply enters here, is classified by an LLM, and only then does a
  # tag get attached that the HUB dispatcher fans out to the children. Adopted 2026-08-17 — it was an
  # orphan, and could not be adopted before because a plaintext OpenAI key in its graph tripped the
  # secret scanner (security finding 12 / defect E6).
  - id: bison-replies-classification
    name: "Bison Replies Classification - Sheets Primary 401 Fallback"
    domain: outreach
    path: automation/n8n/workflows/outreach/bison-replies-classification
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: XdTMd1KJX0cRmF9u
    lastRepositorySync: 2026-08-17

  # Unreachable, and deliberately so. The classifier suppresses the NRR tag before it is attached,
  # so the HUB gate can never fire and this workflow has never executed. Owner decided 2026-08-15 not
  # to revive the email NRR path — kept, not deleted, as the record of what the contract was.
  # Defect C1 in docs/reference/n8n/defect-backlog.md.
  - id: nrr-daily-stats
    name: "[child-2] TAG_ATTACHED · NRR · Daily stats"
    domain: outreach
    path: automation/n8n/workflows/outreach/nrr-daily-stats
    businessProcess: docs/reference/processes/outreach/ooo-followups.md
    owner: automation
    lifecycle: managed
    status: deprecated
    criticality: low
    trigger: sub-workflow
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: 1hHbU2hYYcsktLUP
    lastRepositorySync: 2026-07-21

  # A repeatable repair, not a one-off: it reconciles leads.created_at with the client sheet's
  # LEAD RECEIVED after a write path is fixed forward. Converges — a run with nothing to fix moves
  # nothing. Manual on purpose; a scheduled date rewrite would re-assert the sheet over later fixes.
  - id: sheets-lead-date-backfill
    name: "[BACKFILL] Client Leads sheets → Supabase · lead dates"
    domain: ops
    path: automation/n8n/workflows/ops/sheets-lead-date-backfill
    businessProcess: docs/reference/processes/outreach/sheets-supabase-reconciliation.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: manual
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: UMRS1YJFIcwl6VRK
    lastRepositorySync: 2026-08-03

  # One-off Sheets → Supabase migrations. Ran once on 2026-07-22, then deactivated.
  # Kept under repository control because they are the only record of how the rows got there.
  # docs/reference/processes/outreach/sheets-supabase-reconciliation.md
  - id: sheets-lead-backfill
    name: "[BACKFILL] Sheets → Supabase · leads"
    domain: ops
    path: automation/n8n/workflows/ops/sheets-lead-backfill
    businessProcess: docs/reference/processes/outreach/sheets-supabase-reconciliation.md
    owner: automation
    lifecycle: managed
    status: deprecated
    criticality: medium
    trigger: manual
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: KoLN4bU7qe7RwnGT
    lastRepositorySync: 2026-07-22

  - id: sheets-aimfox-metrics-backfill
    name: "[BACKFILL] Sheets → Supabase · Aimfox daily metrics"
    domain: ops
    path: automation/n8n/workflows/ops/sheets-aimfox-metrics-backfill
    businessProcess: docs/reference/processes/outreach/sheets-supabase-reconciliation.md
    owner: automation
    lifecycle: managed
    status: deprecated
    criticality: medium
    trigger: manual
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: YLfQZBprSRIT6hLm
    lastRepositorySync: 2026-07-22

  - id: sheets-bison-credential-sync
    name: "[CRED] CS PDCA → client_sequencers · Bison keys"
    domain: ops
    path: automation/n8n/workflows/ops/sheets-bison-credential-sync
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: schedule
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: Hzar4pwdAXrDHAwn
    lastRepositorySync: 2026-07-22

  # The edit-driven half of the credential pair. The 6-hourly sweep above can CREATE a Bison row
  # (it reads the client name from the sheet); this one reacts in seconds to a manual edit but only
  # ever resolves a client through the emailbison row that already exists. It is also the only
  # thing that syncs Aimfox keys at all.
  - id: sheets-credential-sync-on-edit
    name: "[CRED] CS PDCA edit → client_sequencers · Bison + Aimfox keys"
    domain: ops
    path: automation/n8n/workflows/ops/sheets-credential-sync-on-edit
    businessProcess: docs/reference/processes/outreach/bison-ingestion.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: webhook
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: ATPnIVnO0sAB9GQx
    lastRepositorySync: 2026-07-29
    lastValidation: 2026-07-29 statement proven in a rolled-back transaction; webhook unauthenticated (security.md #9)

  # Provisioning. Splits the Aimfox half out of the hand-run canvas 8uRWXHe9FIfglq1u ("My workflow
  # 6"), which hardcodes one workspace id, POSTs unconditionally and reads no vendor state — so it
  # can only be run once per client, by hand, and a second run would duplicate every webhook.
  # Iteration 1 (read-only) was proven on five real clients before iteration 2 added the writes.
  # Exactly two nodes POST — Mint Key and Create Missing — and a dry run cannot reach either: Plan
  # Writes returns an empty array, and n8n does not execute a node with no input items. Still
  # INACTIVE and called by hand; the webhook arrives with the gateway action (ADR-0018).
  - id: aimfox-workspace-setup
    name: "[SETUP] Aimfox workspace · resolve, key, webhooks, labels"
    domain: ops
    path: automation/n8n/workflows/ops/aimfox-workspace-setup
    businessProcess: docs/reference/processes/ops/workspace-provisioning.md
    owner: automation
    lifecycle: managed
    status: planned
    criticality: high
    trigger: webhook + executeWorkflowTrigger
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: ehhFUR3SYIxDahER
    lastRepositorySync: 2026-08-07
    lastValidation: 2026-08-07 write path proven on production (executions 70487-70490) — dry runs on Kaizen touched nothing; FortumEnergia dry_run:false created its missing preMQL label and the repeat created nothing (invariant 3). recorded:false at the time because the migration was local-only; closed 2026-08-08 when PR #28 merged and db-migrate ran — re-checked live, recorded:true on both sequencers

  # The Bison half of the same split. Contracts and manifest first, graph second (workflow-lifecycle
  # section C). Its canonical set is NOT the Aimfox one: measured across all 16 Active Bison
  # workspaces, the tags we create are preMQL + OOO only. MQL sits at 7/16 and is not a gap — the
  # Bison MQL path runs off the built-in `Interested` tag, so creating MQL would add a second,
  # less-wired route. The three OOO automation campaigns ARE created, as DRAFTS — name and type,
  # then a schedule, and nothing else. What was blocked was never the campaign but its copy:
  # POST /campaigns/v1.1/{id}/sequence-steps is never called, a manager writes the sequence and
  # starts the campaign, and a draft with no sequence cannot send. `state` still excludes the step
  # so that `configured` never means "ready to send".
  - id: bison-workspace-setup
    name: "[SETUP] Bison workspace · resolve, key, webhooks, tags"
    domain: ops
    path: automation/n8n/workflows/ops/bison-workspace-setup
    businessProcess: docs/reference/processes/ops/workspace-provisioning.md
    owner: automation
    lifecycle: managed
    status: planned
    criticality: high
    trigger: webhook + executeWorkflowTrigger
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: c82kKnHaREUMvPBR
    lastRepositorySync: 2026-08-20
    lastValidation: 2026-08-07 read path proven on production (executions 70463-70467, dry_run only) — Kaizen/Bent Iron/GIC/Audytel configured, Fortum partial (no OOO campaigns at all); canon measured read-only across 16 Active workspaces (webhooks 16/16 both, preMQL 16/16, OOO 16/16, MQL 7/16). Iteration 2 (write half) deployed the same day; not yet run with dry_run:false. 2026-08-08 gained an UNAUTHENTICATED webhook trigger at the owner's direction (knownViolations, review 2026-11-30), activated and proven live — executions 70751/70752/70753 confirmed the fail-safe default, a string 'false', and state client_not_found; after the 2026-08-08 merge a further dry run returned recorded:true 2026-08-20 the OOO routing half deployed surgically to c82kKnHaREUMvPBR (6 nodes updated, `Recover Parked` added with the Postgres credential copied from the live `Record` node, `Record` rewired through it), with post-write verification that no untargeted node, edge or failure posture moved. The artifact was re-exported afterwards, which also reconciled the long-standing `Needs Schedules?` typeVersion drift to what the instance actually runs. Still never executed with dry_run:false, so `status` stays `planned`. 2026-08-21 `Needs Schedules?` was found to carry no `conditions.options` — the artifact was fixed but deliberately NOT deployed, because this workflow is not in service yet (defect-backlog B9).

  - id: crms-lead-dispatcher
    name: "[HUB] CRMs Add/Update Lead Dispatcher"
    domain: crm
    path: automation/n8n/workflows/crm/crms-lead-dispatcher
    businessProcess: docs/reference/functional/11-integrations.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: high
    trigger: executeWorkflowTrigger
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: mfmMYQqK73Nsx6uO
    lastRepositorySync: 2026-08-28
    lastValidation: >-
      2026-08-28 imported ahead of the ADR-0019 rewire — an orphan until then. Reading the five
      children established what each actually consumes, which surfaced two defects the sheet had
      hidden: LiveSpace needs `salt` and `subdomain`, and the sheet has no Salt column while its
      Subdomain column is empty for both LiveSpace rows; Salesforce needs `login_url`, which is
      mapped from a sheet column while the value lives in the data table's `domain`. Both are
      recorded as defect-backlog G4/G5; reading the children settled both, so all 9 connections were
      seeded (ws 77 parked at the owner's direction). The graph
      itself was then REWIRED the same day (approved production write): one Postgres node calling
      resolve_crm_connection replaced the Sheets lookup, the Data Table read, the Merge, the Aggregate
      and an 80-line Code expression, and `Route by CRM type` gained a fallback output wired to
      `Unroutable — record and fail` (defect G1). Post-write verified by re-export: 11 nodes reachable
      from the trigger, the 4 old nodes left dead for a one-edge revert, Postgres credential bound,
      active=true, drift 0. Not yet exercised by a real positive reply.

  - id: zoho-upsert-account-and-lead
    name: "[CRM child] Zoho · Upsert Account + Lead"
    domain: crm
    path: automation/n8n/workflows/crm/zoho-upsert-account-and-lead
    businessProcess: docs/reference/functional/11-integrations.md
    owner: automation
    lifecycle: managed
    status: active
    criticality: medium
    trigger: executeWorkflowTrigger
    environments:
      development:
        remoteWorkflowId: null
      production:
        remoteWorkflowId: am3gYNrZSTbrkRFa
    lastRepositorySync: 2026-08-26
    lastValidation: >-
      2026-08-26 imported to repair it — an orphan until then. defect-backlog B3 recorded
      "expired OAuth token, no lead has reached Zoho"; the execution data says otherwise. [35] refresh,
      [101] Upsert Account and [102] Upsert Lead all succeed on every run — only [103] Add email to
      lead fails, and not for an auth reason: it read $json.access_token from [102]'s response
      (undefined), and its URL and body embedded expressions inside JS string literals so n8n sent the
      source text. Three field bugs sat underneath (last_reply_body_html does not exist, `sent: True`
      was dropped by JSON.stringify, microsecond timestamps). One node re-deployed 2026-08-26, drift 0.
      Behavioural proof still owed — it needs a real positive reply for a Zoho-authorised client.
