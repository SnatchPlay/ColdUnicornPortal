{
  "name": "[SHADOW] Aimfox AutoConnect import · Supabase branch S",
  "nodes": [
    {
      "id": "d1000000-0000-4000-a000-000000000007",
      "name": "Build Plan",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        620,
        0
      ],
      "parameters": {
        "jsCode": "// What branch S WOULD send. No HTTP node exists in this workflow, so this is the whole output.\nconst base = $('Resolve Campaigns').first().json;\nconst rows = $input.all().map((i) => i.json ?? {}).filter((r) => r.lead_id);\nconst byClient = new Map(base.resolved.map((r) => [r.client_id, r]));\n\nconst planned = rows.map((r) => ({\n  client: byClient.get(r.client_id)?.client_name ?? r.client_id,\n  campaign_id: byClient.get(r.client_id)?.campaign_id ?? null,\n  lead_id: r.lead_id,\n  profile_url: r.linkedin_url,\n}));\n\nconst per_client = {};\nfor (const p of planned) per_client[p.client] = (per_client[p.client] ?? 0) + 1;\n\n// No api_key anywhere in here, and this object is what goes into the audit row.\nreturn [{ json: {\n  window: { since: base.since, until: base.until },\n  clients_seen: base.clients_seen,\n  clients_resolved: base.resolved.length,\n  blocked: base.blocked,\n  would_send: planned.length,\n  per_client,\n  sample: planned.slice(0, 20),\n} }];"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000009",
      "name": "Final Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        900,
        0
      ],
      "parameters": {
        "jsCode": "const describe = (v) => {\n  if (v == null) return null;\n  if (typeof v === 'string') return v;\n  return v.message ?? v.description ?? (() => { try { return JSON.stringify(v); } catch (e) { return String(v); } })();\n};\nconst plan = $('Build Plan').first().json;\nconst rec = $json ?? {};\nconst failed = Boolean(rec.error);\nreturn [{ json: { ...plan, sent: 0, mode: 'shadow', recorded: !failed,\n  ...(failed ? { record_error: describe(rec.message ?? rec.error) } : {}) } }];"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000002",
      "name": "Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -80,
        0
      ],
      "parameters": {
        "jsCode": "// One window, computed once, so every query and the audit row agree. Yesterday in UTC, matching\n// branch L's intent — it compares a sheet cell against `$now.minus({days:1})`. Widening this to the\n// whole backlog is a business decision, not a port: on 2026-08-09 that backlog was ~670 people, and\n// each row is a LinkedIn connection request to one of them.\nconst raw = $json ?? {};\nconst end = new Date(Date.UTC(new Date().getUTCFullYear(), new Date().getUTCMonth(), new Date().getUTCDate()));\nconst start = new Date(end.getTime() - 24 * 60 * 60 * 1000);\nreturn [{ json: {\n  client_id: raw.client_id ?? '',\n  since: raw.since ?? start.toISOString(),\n  until: raw.until ?? end.toISOString(),\n} }];"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000004",
      "name": "List Campaigns",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        200,
        0
      ],
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.api_key }}"
            }
          ]
        },
        "options": {},
        "sendHeaders": true,
        "url": "https://api.aimfox.com/api/v2/campaigns"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000003",
      "name": "Read Clients",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        60,
        0
      ],
      "alwaysOutputData": true,
      "parameters": {
        "operation": "executeQuery",
        "options": {
          "queryReplacement": "={{ [ $json.client_id, $json.since, $json.until ] }}"
        },
        "query": "-- The key comes from client_sequencers, not a spreadsheet cell (defect 7 of branch L). It is used\n-- to call the vendor and MUST NOT reach the audit row.\nselect c.id::text                as client_id,\n       c.name                    as client_name,\n       cs.api_key                as api_key,\n       cs.external_workspace_id  as workspace_id\n  from public.clients c\n  join public.client_sequencers cs on cs.client_id = c.id\n  join public.sequencers s on s.id = cs.sequencer_id and s.key = 'aimfox'\n where cs.enabled\n   and cs.api_key is not null and cs.api_key <> ''\n   and c.status in ('Active', 'Onboarding', 'Subscription')\n   and ($1 = '' or c.id = nullif($1, '')::uuid)\n order by c.name;"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000006",
      "name": "Read Leads",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        480,
        0
      ],
      "alwaysOutputData": true,
      "parameters": {
        "operation": "executeQuery",
        "options": {
          "queryReplacement": "={{ [ JSON.stringify($json.resolved.map(r => r.client_id)), $json.since, $json.until ] }}"
        },
        "query": "-- `linkedin_invitation_sent_at is null` is a local idempotency key branch L never had (defect 5):\n-- it re-reads yesterday every run and re-POSTs the batch. The column exists and nothing writes it\n-- today, so branch S can own it once it starts sending.\n-- The window is a real timestamptz range, not a string compared as M/d/yyyy (defect 3).\nselect l.id::text        as lead_id,\n       l.client_id::text as client_id,\n       l.linkedin_url    as linkedin_url\n  from public.leads l\n where l.client_id in (select (jsonb_array_elements_text($1::jsonb))::uuid)\n   and l.linkedin_url is not null and l.linkedin_url <> ''\n   and l.linkedin_invitation_sent_at is null\n   and l.created_at >= $2::timestamptz\n   and l.created_at <  $3::timestamptz\n order by l.client_id, l.created_at;"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000008",
      "name": "Record Run",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        760,
        0
      ],
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "operation": "executeQuery",
        "options": {
          "queryReplacement": "={{ [ String($execution.id), $json.would_send > 0 ? 'success' : 'success', JSON.stringify($json) ] }}"
        },
        "query": "-- Branch L records nothing at all (defect 6), which is how a nightly job imported for nobody for\n-- months without anyone noticing. A shadow that is not recorded is not a measurement.\ninsert into public.integration_sync_runs\n  (provider, sync_type, n8n_execution_id, status, started_at, finished_at, metadata)\nvalues ('aimfox', 'autoconnect_import_shadow', $1, $2, now(), now(), $3::jsonb)\non conflict (n8n_execution_id) do nothing\nreturning id::text as run_id;"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000005",
      "name": "Resolve Campaigns",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        340,
        0
      ],
      "parameters": {
        "jsCode": "// Collapses N per-client responses into ONE item, so every node downstream runs exactly once.\n// n8n preserves item order, so the i-th response belongs to the i-th client.\nconst clients = $('Read Clients').all().map((i) => i.json);\nconst responses = $input.all().map((i) => i.json ?? {});\n\n// Matched by name across the WHOLE list. Branch L reads campaigns[0] and nothing else, which is\n// why it imported for 0 of 9 clients on 2026-08-09 (defect 1).\nconst resolved = [];\nconst blocked = [];\nclients.forEach((c, i) => {\n  const raw = responses[i] ?? {};\n  if (raw.error || raw.status === 'fail') {\n    blocked.push({ client: c.client_name, reason: 'vendor call failed' });\n    return;\n  }\n  const all = raw.campaigns ?? [];\n  const found = all.find((x) => String(x?.name ?? '') === 'AutoConnect');\n  if (!found) { blocked.push({ client: c.client_name, reason: 'no AutoConnect campaign' }); return; }\n  if (found.state !== 'ACTIVE') { blocked.push({ client: c.client_name, reason: `AutoConnect is ${found.state}` }); return; }\n  resolved.push({ client_id: c.client_id, client_name: c.client_name, campaign_id: found.id });\n});\n\nreturn [{ json: {\n  ...$('Input').first().json,\n  clients_seen: clients.length,\n  resolved,\n  blocked,\n} }];"
      }
    },
    {
      "id": "d1000000-0000-4000-a000-000000000001",
      "name": "Start",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        -200,
        0
      ],
      "parameters": {}
    }
  ],
  "connections": {
    "Build Plan": {
      "main": [
        [
          {
            "index": 0,
            "node": "Record Run",
            "type": "main"
          }
        ]
      ]
    },
    "Input": {
      "main": [
        [
          {
            "index": 0,
            "node": "Read Clients",
            "type": "main"
          }
        ]
      ]
    },
    "List Campaigns": {
      "main": [
        [
          {
            "index": 0,
            "node": "Resolve Campaigns",
            "type": "main"
          }
        ]
      ]
    },
    "Read Clients": {
      "main": [
        [
          {
            "index": 0,
            "node": "List Campaigns",
            "type": "main"
          }
        ]
      ]
    },
    "Read Leads": {
      "main": [
        [
          {
            "index": 0,
            "node": "Build Plan",
            "type": "main"
          }
        ]
      ]
    },
    "Record Run": {
      "main": [
        [
          {
            "index": 0,
            "node": "Final Result",
            "type": "main"
          }
        ]
      ]
    },
    "Resolve Campaigns": {
      "main": [
        [
          {
            "index": 0,
            "node": "Read Leads",
            "type": "main"
          }
        ]
      ]
    },
    "Start": {
      "main": [
        [
          {
            "index": 0,
            "node": "Input",
            "type": "main"
          }
        ]
      ]
    }
  },
  "settings": {
    "callerPolicy": "workflowsFromSameOwner",
    "errorWorkflow": "Pmz0JjRRuJNdNpSE",
    "executionOrder": "v1"
  }
}
