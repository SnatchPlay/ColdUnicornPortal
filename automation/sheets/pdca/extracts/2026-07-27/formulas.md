# CS PDCA — formula templates (snapshot 2026-07-27)

Generated by `pnpm sheets:extract`. **Do not hand-edit.**

One entry per column, taken from the first client row that carries a formula; every other
client row is the same formula with its own row number. Google-Sheets-only functions are
unwrapped from the `__xludf.DUMMYFUNCTION` envelope the .xlsx export puts them in.

## N — Basic · Inboxes

```
IF(G4="Active", 
   IF(OR(E4<>"", C4<>""),  
      IFERROR(
         INDEX(
            FILTER('🤖Daily stats'!J:J, 
                   ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4), 
                   '🤖Daily stats'!I:I = TODAY()-1),
            1
         ),
         0
      ), 
      ""
   ),
   ""
)
```

## P — cold emails · Prospects Added

```
IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!K:K, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!K:K, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), "")
```

## Q — Min Sent

```
O4*3/20
```

## U — Gheads response time - emails · DoD Schedule
for cold @ & LI · +2

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AA:AA, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!AA:AA, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AE:AE, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!AE:AE, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "")
```

## V — +1

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!Z:Z, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!Z:Z, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AD:AD, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!AD:AD, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "")
```

## W — (unlabelled)

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!Y:Y, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!Y:Y, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AC:AC, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!AC:AC, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "")
```

## X — DoD Daily sent
for cold @ & LI

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY())
  +
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()),
  0
), ""), "")
```

## Y — -1

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-1)
  +
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-1),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-1)
  +
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-1),
  0
), ""), "")
```

## Z — -2

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-2)
  +
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-2),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-2)
  +
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-2),
  0
), ""), "")
```

## AA — -3

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-3)
  +
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-3),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-3)
  +
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-3),
  0
), ""), "")
```

## AB — -4

```
IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-4)
  +
  SUMIFS('🤖Daily stats'!B:B, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-4),
  0
), ""), "") + IF(G4="Active", IF(OR(E4<>"", C4<>""), IFERROR(
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, E4, '🤖Daily stats'!I:I, TODAY()-4)
  +
  SUMIFS('🤖Daily stats'!AB:AB, '🤖Daily stats'!A:A, C4, '🤖Daily stats'!I:I, TODAY()-4),
  0
), ""), "")
```

## AC — 3DoD
TOTAL leads received

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY(),
  countMQL,  COUNTIFS(status, "MQL", dates, ">=" & target, dates, "<" & target + 1),
  countPreMQL, COUNTIFS(status, "PreMQL", dates, ">=" & target, dates, "<" & target + 1),
  countMQL + countPreMQL
),"")
```

## AD — -1

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY()-1,
  countMQL,  COUNTIFS(status, "MQL", dates, ">=" & target, dates, "<" & target + 1),
  countPreMQL, COUNTIFS(status, "PreMQL", dates, ">=" & target, dates, "<" & target + 1),
  countMQL + countPreMQL
),"")
```

## AE — -2

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY()-2,
  countMQL,  COUNTIFS(status, "MQL", dates, ">=" & target, dates, "<" & target + 1),
  countPreMQL, COUNTIFS(status, "PreMQL", dates, ">=" & target, dates, "<" & target + 1),
  countMQL + countPreMQL
),"")
```

## AF — -3

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY()-3,
  countMQL,  COUNTIFS(status, "MQL", dates, ">=" & target, dates, "<" & target + 1),
  countPreMQL, COUNTIFS(status, "PreMQL", dates, ">=" & target, dates, "<" & target + 1),
  countMQL + countPreMQL
),"")
```

## AG — -4

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY()-4,
  countMQL,  COUNTIFS(status, "MQL", dates, ">=" & target, dates, "<" & target + 1),
  countPreMQL, COUNTIFS(status, "PreMQL", dates, ">=" & target, dates, "<" & target + 1),
  countMQL + countPreMQL
),"")
```

## AH — 3DoD
SQL leads received

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY(),
  COUNTIFS(
    status,  "MQL",
    dates,   ">=" & target,
    dates,   "<"  & target + 1
  )
),"")
```

## AI — -1

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY() - 1,
  COUNTIFS(
    status,  "MQL",
    dates,   ">=" & target,
    dates,   "<"  & target + 1
  )
),"")
```

## AJ — -2

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY() - 2,
  COUNTIFS(
    status,  "MQL",
    dates,   ">=" & target,
    dates,   "<"  & target + 1
  )
),"")
```

## AK — -3

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY() - 3,
  COUNTIFS(
    status,  "MQL",
    dates,   ">=" & target,
    dates,   "<"  & target + 1
  )
),"")
```

## AL — -4

```
IF(G4="Active",LET(
  id,        D4,
  data,      IMPORTRANGE(id, "Leads!I:N"),
  dates,     INDEX(data,,1),
  status,    INDEX(data,,6),
  target,    TODAY() - 4,
  COUNTIFS(
    status,  "MQL",
    dates,   ">=" & target,
    dates,   "<"  & target + 1
  )
),"")
```

## AM — WoW
Bounce rate

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!G:G,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## AN — -1

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!G:G,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
    ),
    4
  ),
  0
), "")
```

## AO — -2

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!G:G,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## AP — -3

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!G:G,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
    ),
    4
  ),
  0
), "")
```

## AQ — WoW
Total response rate

```
IF(G4="Active",AU4+AY4,"")
```

## AR — -1

```
IF(G4="Active",AV4+AZ4,"")
```

## AS — -2

```
IF(G4="Active",AW4+BA4,"")
```

## AT — -3

```
IF(G4="Active",AX4+BB4,"")
```

## AU — WoW
Human response rate

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          IF('🤖Daily stats'!S:S = "SmartLead", '🤖Daily stats'!F:F, IF('🤖Daily stats'!S:S = "Bizon", '🤖Daily stats'!V:V, 0)),
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## AV — -1

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          IF('🤖Daily stats'!S:S = "SmartLead", '🤖Daily stats'!F:F, IF('🤖Daily stats'!S:S = "Bizon", '🤖Daily stats'!V:V, 0)),
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
    ),
    4
  ),
  0
), "")
```

## AW — -2

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          IF('🤖Daily stats'!S:S = "SmartLead", '🤖Daily stats'!F:F, IF('🤖Daily stats'!S:S = "Bizon", '🤖Daily stats'!V:V, 0)),
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## AX — -3

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          IF('🤖Daily stats'!S:S = "SmartLead", '🤖Daily stats'!F:F, IF('🤖Daily stats'!S:S = "Bizon", '🤖Daily stats'!V:V, 0)),
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
    ),
    4
  ),
  0
), "")
```

## AY — WoW
Out of office rate

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!X:X,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## AZ — -1

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!X:X,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
    ),
    4
  ),
  0
), "")
```

## BA — -2

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!X:X,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## BB — -3

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!X:X,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)  - 14
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
    ),
    4
  ),
  0
), "")
```

## BC — WoW
Invitation acceptance rate

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!T:T,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) + 1,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) + 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## BD — -1

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!T:T,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 6,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)
        )
      )
    ),
    4
  ),
  0
), "")
```

## BE — -2

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!T:T,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 13,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 7
        )
      )
    ),
    4
  ),
  0
), "")
```

## BF — -3

```
IF(G4="Active", IFERROR(
  ROUND(
    (
      SUM(
        FILTER(
          '🤖Daily stats'!T:T,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2)  - 14
        )
      )
      /
      SUM(
        FILTER(
          '🤖Daily stats'!B:B,
          ('🤖Daily stats'!A:A = E4) + ('🤖Daily stats'!A:A = C4),
          '🤖Daily stats'!I:I >= TODAY() - WEEKDAY(TODAY(), 2) - 20,
          '🤖Daily stats'!I:I <= TODAY() - WEEKDAY(TODAY(), 2) - 14
        )
      )
    ),
    4
  ),
  0
), "")
```

## BG — WoW
TOTAL leads received

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:I"),       
    dates,   INDEX(data, , 1),                                 
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) + 1, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) + 7,
    COUNTIFS(
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BH — -1

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:I"),       
    dates,   INDEX(data, , 1),                                      
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -6, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) ,
    COUNTIFS(
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BI — -2

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:I"),       
    dates,   INDEX(data, , 1),                   
                
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -13, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) -7,
    COUNTIFS(

      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BJ — -3

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:I"),       
    dates,   INDEX(data, , 1),                                    
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -20, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) -14,
    COUNTIFS(
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  )
,"")
```

## BK — WoW
SQL leads received

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:N"),       
    dates,   INDEX(data, , 1),                   
    status,  INDEX(data, , 6),                   
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) + 1, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) + 7,
    COUNTIFS(
      status, "MQL",
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BL — -1

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:N"),       
    dates,   INDEX(data, , 1),                   
    status,  INDEX(data, , 6),                   
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -6, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) ,
    COUNTIFS(
      status, "MQL",
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BM — -2

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:N"),       
    dates,   INDEX(data, , 1),                   
    status,  INDEX(data, , 6),                   
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -13, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) -7,
    COUNTIFS(
      status, "MQL",
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  ),"")
```

## BN — -3

```
IF(G4="Active",LET(
    id,      D4,                                
    data,    IMPORTRANGE(id, "Leads!I:N"),       
    dates,   INDEX(data, , 1),                   
    status,  INDEX(data, , 6),                   
    weekStart, TODAY() - WEEKDAY(TODAY(), 2) -20, 
    weekEnd,   TODAY() - WEEKDAY(TODAY(), 2) -14,
    COUNTIFS(
      status, "MQL",
      dates, ">=" & weekStart,
      dates, "<=" & weekEnd
    )
  )
,"")
```

## BU — Gheads growth - emails · MoM
TOTAL leads received

```
IF(G4="Active",LET(
  id,         D4,
  data,       IMPORTRANGE(id, "Leads!I:I"),
  dates,      INDEX(data,,1),
  monthStart, DATE(YEAR(TODAY()), MONTH(TODAY()), 1),
  monthEnd,   EOMONTH(TODAY(), 0),
  COUNTIFS(
    dates, ">=" & monthStart,
    dates, "<=" & monthEnd
  )
)
,"")
```

## BV — -1

```
IF(G4="Active",LET(
  id,           D4,
  data,         IMPORTRANGE(id, "Leads!I:I"),
  dates,        INDEX(data,,1),
  prevMonthStart, EOMONTH(TODAY(), -2) + 1,
  prevMonthEnd,   EOMONTH(TODAY(), -1),
  COUNTIFS(
    dates, ">=" & prevMonthStart,
    dates, "<=" & prevMonthEnd
  )
)
,"")
```

## BW — -2

```
IF(G4="Active",LET(
  id,                  D4,
  data,                IMPORTRANGE(id, "Leads!I:I"),
  dates,               INDEX(data,,1),
  twoMoAgoStart,       EOMONTH(TODAY(), -3) + 1,
  twoMoAgoEnd,         EOMONTH(TODAY(), -2),
  COUNTIFS(
    dates, ">=" & twoMoAgoStart,
    dates, "<=" & twoMoAgoEnd
  )
)
,"")
```

## BX — -3

```
IF(G4="Active",LET(
  id,                    D4,
  data,                  IMPORTRANGE(id, "Leads!I:I"),
  dates,                 INDEX(data,,1),
  threeMoAgoStart,       EOMONTH(TODAY(), -4) + 1,
  threeMoAgoEnd,         EOMONTH(TODAY(), -3),
  COUNTIFS(
    dates, ">=" & threeMoAgoStart,
    dates, "<=" & threeMoAgoEnd
  )
)
,"")
```

## BZ — (unlabelled)

```
IF(G4="Active",LET(
  id,         D4,
  data,       IMPORTRANGE(id, "Leads!I:N"),
  dates,      INDEX(data,,1),
  status,     INDEX(data,,6),
  monthStart, DATE(YEAR(TODAY()), MONTH(TODAY()), 1),
  monthEnd,   EOMONTH(TODAY(), 0),
  COUNTIFS(
    status, "MQL",
    dates, ">=" & monthStart,
    dates, "<=" & monthEnd
  )
)
,"")
```

## CA — -1

```
IF(G4="Active",LET(
  id,           D4,
  data,         IMPORTRANGE(id, "Leads!I:N"),
  dates,        INDEX(data,,1),
  status,       INDEX(data,,6),
  prevMonthStart, EOMONTH(TODAY(), -2) + 1,
  prevMonthEnd,   EOMONTH(TODAY(), -1),
  COUNTIFS(
    status, "MQL",
    dates, ">=" & prevMonthStart,
    dates, "<=" & prevMonthEnd
  )
)
,"")
```

## CB — -2

```
IF(G4="Active",LET(
  id,                  D4,
  data,                IMPORTRANGE(id, "Leads!I:N"),
  dates,               INDEX(data,,1),
  status,              INDEX(data,,6),
  twoMoAgoStart,       EOMONTH(TODAY(), -3) + 1,
  twoMoAgoEnd,         EOMONTH(TODAY(), -2),
  COUNTIFS(
    status, "MQL",
    dates, ">=" & twoMoAgoStart,
    dates, "<=" & twoMoAgoEnd
  )
)
,"")
```

## CC — -3

```
IF(G4="Active",LET(
  id,                    D4,
  data,                  IMPORTRANGE(id, "Leads!I:N"),
  dates,                 INDEX(data,,1),
  status,                INDEX(data,,6),
  threeMoAgoStart,       EOMONTH(TODAY(), -4) + 1,
  threeMoAgoEnd,         EOMONTH(TODAY(), -3),
  COUNTIFS(
    status, "MQL",
    dates, ">=" & threeMoAgoStart,
    dates, "<=" & threeMoAgoEnd
  )
)
,"")
```

## CE — (unlabelled)

```
IF(G4="Active",LET(
  id,            D4,
  data,          IMPORTRANGE(id, "Leads!I:Q"),    
  dates,         INDEX(data,,1),                  
  meeting,       INDEX(data,,8),                  
  monthStart,    DATE(YEAR(TODAY()), MONTH(TODAY()), 1),
  monthEnd,      EOMONTH(TODAY(), 0),
  COUNTIFS(
    meeting, TRUE,                                 
    dates, ">=" & monthStart,
    dates, "<=" & monthEnd
  )
)
,"")
```

## CF — -1

```
IF(G4="Active",LET(
  id,               D4,
  data,             IMPORTRANGE(id, "Leads!I:Q"),
  dates,            INDEX(data,,1),
  meeting,          INDEX(data,,8),
  prevMoStart,      EOMONTH(TODAY(), -2) + 1,
  prevMoEnd,        EOMONTH(TODAY(), -1),
  COUNTIFS(
    meeting, TRUE,
    dates, ">=" & prevMoStart,
    dates, "<=" & prevMoEnd
  )
)
,"")
```

## CG — -2

```
IF(G4="Active",LET(
  id,               D4,
  data,             IMPORTRANGE(id, "Leads!I:Q"),
  dates,            INDEX(data,,1),
  meeting,          INDEX(data,,8),
  prevMoStart,      EOMONTH(TODAY(), -3) + 1,
  prevMoEnd,        EOMONTH(TODAY(), -2),
  COUNTIFS(
    meeting, TRUE,
    dates, ">=" & prevMoStart,
    dates, "<=" & prevMoEnd
  )
)
,"")
```

## CH — -3

```
IF(G4="Active",LET(
  id,               D4,
  data,             IMPORTRANGE(id, "Leads!I:Q"),
  dates,            INDEX(data,,1),
  meeting,          INDEX(data,,8),
  prevMoStart,      EOMONTH(TODAY(), -4) + 1,
  prevMoEnd,        EOMONTH(TODAY(), -3),
  COUNTIFS(
    meeting, TRUE,
    dates, ">=" & prevMoStart,
    dates, "<=" & prevMoEnd
  )
)
,"")
```

## CJ — (unlabelled)

```
IF(G4="Active",LET(
  id,            D4,
  data,          IMPORTRANGE(id, "Leads!I:T"),
  dates,         INDEX(data,,1),    
  won,           INDEX(data,,10),   
  monthStart,    DATE(YEAR(TODAY()), MONTH(TODAY()), 1),
  monthEnd,      EOMONTH(TODAY(), 0),
  COUNTIFS(
    won, TRUE,
    dates, ">=" & monthStart,
    dates, "<=" & monthEnd
  )
)
,"")
```

## CK — -1

```
IF(G4="Active",LET(
  id,               D4,
  data,             IMPORTRANGE(id, "Leads!I:T"),
  dates,            INDEX(data,,1),
  won,           INDEX(data,,10),
  prevMoStart,      EOMONTH(TODAY(), -2) + 1,
  prevMoEnd,        EOMONTH(TODAY(), -1),
  COUNTIFS(
    won, TRUE,
    dates, ">=" & prevMoStart,
    dates, "<=" & prevMoEnd
  )
)
,"")
```

## CL — -2

```
IF(G4="Active",LET(
  id,                  D4,
  data,                IMPORTRANGE(id, "Leads!I:T"),
  dates,               INDEX(data,,1),
  won,           INDEX(data,,10),
  twoMoAgoStart,       EOMONTH(TODAY(), -3) + 1,
  twoMoAgoEnd,         EOMONTH(TODAY(), -2),
  COUNTIFS(
    won, TRUE,
    dates, ">=" & twoMoAgoStart,
    dates, "<=" & twoMoAgoEnd
  )
)
,"")
```

## CM — -3

```
IF(G4="Active",LET(
  id,                    D4,
  data,                  IMPORTRANGE(id, "Leads!I:T"),
  dates,                 INDEX(data,,1),
  won,           INDEX(data,,10),
  threeMoAgoStart,       EOMONTH(TODAY(), -4) + 1,
  threeMoAgoEnd,         EOMONTH(TODAY(), -3),
  COUNTIFS(
    won, TRUE,
    dates, ">=" & threeMoAgoStart,
    dates, "<=" & threeMoAgoEnd
  )
)
,"")
```
